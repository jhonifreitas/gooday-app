class Base {
  final String? id;
  final DateTime? createdAt;
  final DateTime? updatedAt;
  final DateTime? deletedAt;

  const Base({
    this.id,
    this.createdAt,
    this.updatedAt,
    this.deletedAt,
  });
}
